networks:
  spotify-network:
    name: spotify-network
    driver: bridge

services:
  spotify-backofficep:
    container_name: spotify-backofficep
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    image: spotify-backofficep:latest
    command: sh -c "npm install && npm run dev"
    networks:
      - spotify-network
    ports:
      - "3001:3000"
    volumes:
      - .:/app
      - /app/node_modules
